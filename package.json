{
    "name": "@links/monorepo",
    "private": true,
    "version": "0.0.1",
    "workspaces": {
        "packages": [
            "packages/*",
            "services/*"
        ]
    },
    "scripts": {
        "api:start:dev": "cd services/api && yarn run start:dev",
        "web:start:dev": "cd services/web && yarn run start:dev"
    }
}
